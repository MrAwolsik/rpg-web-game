{
  "name": "rpg-web-game",
  "version": "1.0.0",
  "scripts": {
    "start": "concurrently -n BACKEND,FRONTEND -c yellow,cyan \"npm run server\" \"npm run client\"",
    "server": "cd backend && node index.js",
    "client": "cd frontend && npm start"
  },
  "devDependencies": {
    "concurrently": "^8.2.0"
  }
}
